apply plugin: 'java'
apply plugin: 'application'
apply plugin: 'eclipse'

sourceSets {
	main {
		java {
			srcDirs = ['src']
		}
	}
}

dependencies {
}

mainClassName = "network.Main"

buildDir = "dist"